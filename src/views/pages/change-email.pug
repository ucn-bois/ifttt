extends ../base

block content
    h1 Change email
    form(action=`/change-email`, method='POST')
        input(type='hidden', name='_csrf', value=csrfToken)
        label(for='newEmail') New email
        input#newEmail(type='text' name='newEmail', required)
        button(type='submit') Change email